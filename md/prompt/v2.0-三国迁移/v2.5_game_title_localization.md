# v2.5 游戏标题纯中文显示收敛

## 目标

继续推进发布级三国 UI 收口，把默认 HUD 和 SpriteKit 地图标题中的英文 `Agent` 去掉，玩家第一眼看到纯中文题名“ 三国棋策 ”。

## 范围

- 修改 `SanguoDisplayLexicon.gameTitle`。
- 影响 `HUDView` 和 `BoardScene` 读取的标题显示。
- 同步 `README.md`、`md/flow/flow.md`、`md/flow/flowchart.md`、`md/prompt/README.md` 和 `update_log.md`。

## 非目标

- 不重命名 bundle、target、仓库、源码目录或 `WWIIHexV0` 工程名。
- 不修改 AI Agent 架构、提示词 schema、记录类型或调试字段。
- 不替换文档中作为产品路线或架构术语出现的 “AI Agent”。
- 不改规则、地图、数据、道路、粮道、交战、武将或胜负判断。

## 架构依据

- `SanguoDisplayLexicon.gameTitle` 是玩家可见标题的共享入口。
- `HUDView` 和 `BoardScene` 均读取该字段，不需要分别改 UI。
- 项目总目标要求发布前主 UI 不出现不必要的英文残留；本切片只收口玩家标题，不改变开发/架构术语。

## 轻量检查

- `swiftc -parse WWIIHexV0/Core/Faction.swift`
- 定向扫描 `SanguoDisplayLexicon.gameTitle` 是否仍含 `Agent`。
- 本轮改动文件尾随空白扫描。
- 本轮改动文件冲突标记扫描。
- prompt 索引差集检查。
- `git diff --check`。
